pipeline {
    agent any

    stages {

    	stage('build') {
    		steps {
                	sh 'npm --version'
    		}
    	}
    	stage('Run Docker Compose File')
    	{
        	sh 'sudo docker-compose build'
        	sh 'sudo docker-compose up -d'
    	}

    	stage('Test') {
        	steps {
                	echo 'Testing..'
        	}
    	}
    	stage('Deploy') {
    		steps {
                	echo 'Deploying....'
        	}
    	}
   }
}